#include <stdio.h>
#include <stdlib.h>
#include "supporto.h"

int main()
{


    player_vs_player(1);
   /* tplayer *p1 = NULL;
    tcampo *t = NULL;
    t = create_board(7,7,6);
    p1 = create_pawns(11,'B',1,2,*t);

    print_player(*p1);
    destroy_player(p1);
    destroy_board(t);*/
    return 0;
}

